<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
		<link rel="stylesheet" href="/assets/css/style.css" type="text/css" />
		<title>EAT DA BURGER!</title>
		<script src="https://code.jquery.com/jquery.js"></script>
		<script src="/assets/js/burgers.js"></script>
	</head>


<h1>EAT A BURGER</h1>

<h2>EAT THESE</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>BURGERS TO EAT</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Name a burger:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Did you eat it?</label><br>
    <input type="radio" name="devoured" value="1" checked> EATEN!<br>
    <input type="radio" name="devoured" value="0"> Not Hungry
  </div>

  <button type="submit">Add Your Burger</button>
</form>
